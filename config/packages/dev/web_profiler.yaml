web_profiler:
    #  Invalid type for path "web_profiler.toolbar". Expected boolean, but got string.
    toolbar: "%profiler_toolbar%"
    intercept_redirects: false

framework:
    profiler: { only_exceptions: false }
